generator client {
  provider = "prisma-client-js"
  output   = "../../node_modules/@prisma/usuarios-client"
}

datasource db {
  provider = "sqlite"
  url      = "file:./usuarios.db"
}

model Usuario {
  id       Int      @id @default(autoincrement())
  nome     String
  funcao   Funcao
  login    String   @unique
  senha    String
  ativo    Boolean  @default(true)
  createdAt DateTime @default(now())
}

enum Funcao {
  CADASTRO     // Responsável A
  COMPRAS      // Responsável B
  RECEBIMENTO  // Responsável C
  FUNCIONARIO  // Responsável D
  EMPREGADA
  GESTOR
}
